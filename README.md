# Inicializar
* git init
* git add .  o  git add FILENAME
* git commit -m "NAME"
* git remote add origin URL
* git push -u origin master

# Clonar
* git clone URL

# Modificar
* git pull
* git push

# Revisar
* git status